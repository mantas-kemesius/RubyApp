# frozen_string_literal: true

require 'rails_helper'

RSpec.describe UserController, type: :controller do
  # let(:UserControl) do
  #   UserControl = described_class
  #   allow_any_instance_of(UserControl).to receive(:login).and_return(true)
  #   UserControl
  # end
  #
  # let(:login_hash) do
  #   { user: { email: 'test@gmail.com', password: 'p4ssw1rd' } }
  # end
  #
  # it 'signs user in if credentials are valid' do
  #   hash1 = { 'user' => { 'email' => 'samuolaitis@gmail.com',
  #                         'password' => 'slaptazodisxd' } }
  #   expect_any_instance_of(UserControl).to receive(:proceed_login)
  #   post :proceed_login, params: hash1
  # end
end
