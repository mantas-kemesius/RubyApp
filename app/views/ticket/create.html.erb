<div class="content-container default-content-container">
<%= form_for :ticket, :url => { :action => 'create' }, :html=>{:class=>"user-form"} do |form| %>
    <p>
      <%= form.label :ticket %><br>
      <%= form.text_field :ticket %>
    </p>

    <p>
      <%= form.submit 'Add' %>
    </p>
  <% end %>
</div>
